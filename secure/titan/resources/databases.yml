vars:
  - name: databases
    type: list
    default:
      - raw
      - balboa
      - balboa_apps
      - balboa_dev
      - snowflake
      - covid19_epidemiological_data
      - great_bay
      - sushi_db

databases:
  # Titan does not support the for_each syntax on databases yet
  # - for_each: var.databases
  #   name: "{{ each.value }}"
  - name: raw
  - name: balboa
  - name: balboa_apps
  - name: balboa_dev
  - name: covid19_epidemiological_data
  - name: great_bay
  - name: sushi_db
