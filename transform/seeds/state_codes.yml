version: 2

seeds:
  - name: state_codes
    description: Lookup table used to convert between state abbreviations and state names
    config:
      access: public
    columns:
      - name: state_name
        description: Name of the state
      - name: state_code
        description: Two letter abbreviation for the state
