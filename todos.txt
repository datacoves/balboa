pip install dbt-osmosis
pip install dbt-osmosis==0.7.5
pip install --upgrade dbt-osmosis==0.7.5

dbt-osmosis server --port 8582 --host localhost

pip install shandy-sqlfmt[jinjafmt]


Alex
- add panel for dbt tests in the dbt power user panel
- Real Time compilation panel


Airflow on Balboa
- add sample Airflow job (like orrum)


sqlfluff
- figure out if we can put osmosis as the backend


VS Code settings updates
- enable Insert final new line
- enable trim final new line
- enable trim final whitespace

- turn on execute in terminal
